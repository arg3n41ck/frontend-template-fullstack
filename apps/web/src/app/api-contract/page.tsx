export default function ApiContractPage() {
  return (
    <main className="mx-auto min-h-svh max-w-3xl p-6 sm:p-12">
      <h1 className="text-3xl font-semibold tracking-tight">API contract boundary</h1>
      <p className="mt-4 text-muted-foreground">Share serializable types through packages/contracts. Keep validation, persistence and secrets inside apps/api.</p>
    </main>
  );
}
